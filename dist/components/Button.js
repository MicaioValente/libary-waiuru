import React from 'react';
import { View, Text } from 'react-native';
const Button = () => (React.createElement(View, null,
    React.createElement(Text, null, "Meu Componente")));
export default Button;
